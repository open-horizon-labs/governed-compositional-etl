# Review 8 (sketch reviewer, scoped): trade-lifecycle on duckdb-sqlmesh, L3 cycle 8 (verdict: pass)

Review 7's findings closed: the absence branch restored in the widened form; inv.trade_order_type_frozen as the L2 states it. The widened hold implemented as the L2 states it (earliest report of either source held, or earliest raw.trade_cdc report held; all-history-held holds the trade; a held report's place in the order read, nothing else), provably the same set as the native gate. inv.trade_held_first_report_unclaimed restates the L2 and reads nothing beyond position. Evidence: check ok (13 audits); run 13 zero; two-phase ok; mutate zero unprotected; held-for-review simulation five plus one rows; market-order silent; compare identical. Rejected artifacts: none. Stamped on acceptance.
