# Change contract, L3 cycle 6: trade-lifecycle on duckdb-sqlmesh (unknown codes are held, never interpreted)

- **Prior policy authority:** the previous selection your accepted cycle compiled from.
- **Exact active change authority:** the re-selected L2 (take model_sha256 from `chain/l2/trade-lifecycle/review.json`), compiled under the Sketch's new clause `L1.unknown-codes` (a fixed vocabulary; an unknown code is held for review, never interpreted, defaulted or dropped; every job reports it naming the record and the code) and the new hole `L1.hole.market-order-seen-pending`. Read the selected model for the new group `sg.unknown-codes` and its invariant, and every element whose parallel_assumption now says an unknown value yields no derived fact.
- **Stale artifacts:** Re-project the entity: first_seen_late is now nullable and null means held (a market order whose first-encountered report is PNDG, or any unknown code on the first-encountered report); no rank arithmetic may reach those cases. Audits: `inv.unknown_codes_held` (rows of raw.trade_cdc and raw.trade_history whose t_st_id/th_st_id, t_tt_id or cdc_flag carry a value outside the anchored vocabulary, naming record and code), `inv.trade_market_order_seen_pending_held` (every market order whose first-encountered report is PNDG), `inv.trade_first_seen_late_defined_or_held` (null exactly on the held set), `inv.trade_order_type_frozen` (its own file now; remove that clause from the first-seen-late audit), and `inv.trade_first_seen_late_matches_status_order` restated over defined cases. Then `simulate <target> trade-lifecycle --ce counterexamples/proposed/ce-held-for-review-trades-v1.json` must fire exactly the two held audits naming 900004 (market order seen pending) and 900005 (type TXX), and `simulate ... --ce counterexamples/proposed/ce-market-order-first-seen-submitted-v1.json` must stay silent with 900002 not late and 900003 late.
- **Forbidden:** an ELSE branch or default for an unknown code; dropping a row because of an unknown code; deciding what a held record means afterwards.
- **Acceptance:** `chain_l3.py check duckdb-sqlmesh trade-lifecycle` ok or question with zero problems and one audit per selected deterministic invariant; `run` ok with every audit at zero on the fixture; `twophase` ok; `mutate` zero unprotected; the simulations named above. Report briefly.
