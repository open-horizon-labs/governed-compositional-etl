# Change contract, L3 cycle 8: trade-lifecycle on duckdb-sqlmesh (held reports as evidence; the absence audit)

- **Exact active change authority:** the re-selected L2 (model_sha256 from `chain/l2/trade-lifecycle/review.json`, cycle 16) and `review-7.md` in this directory. Sketch: a held report keeps its place in the order of reports; nothing else about it may be read; a trade any of whose placement-fixing facts (placement, owning account, order type) would come from a held report is held.
- **Stale artifacts:** the entity SQL: a trade is unclaimed (no row) when the earliest report of either source is held OR the earliest raw.trade_cdc report (the source of owning account and order type) is held, whatever the earliest history row says; when every history row is held the earliest history row still orders first and the trade is held. Audits: new `inv.trade_held_first_report_unclaimed` (such trades have no row); `inv.trade_order_type_frozen` as the L2 now states it (first-encountered anchored report's t_tt_id, and every anchored report agrees); `inv.every_received_trade_persisted` with the absence branch on both engines; the three first-seen-late audits read raw.trade_history only where th_st_id is anchored (no COALESCE over a held row's status) and use null-sensitive held tests; `inv.trade_owning_account_frozen`, `inv.trade_placement_reference_frozen` as restated. Leave group_fingerprints alone.
- **Acceptance:** check ok (13 audits); run 13 zero; twophase; mutate zero unprotected; held-for-review simulation: inv.unknown_codes_held five rows (900005, 900006 later, 900007, 900008 first, 900009 first CDC) and inv.trade_market_order_seen_pending_held one (900004); 900006 at PNDG; 900005, 900007, 900008, 900009 no row; market-order simulation silent (900002 not late, 900003 late). On SQLMesh confirm the second audit by direct SQL. Report the rows.
