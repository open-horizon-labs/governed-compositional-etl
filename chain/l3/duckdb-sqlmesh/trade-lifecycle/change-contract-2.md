# Change contract, L3 cycle 2: trade-lifecycle on duckdb-sqlmesh

- **Prior policy authority:** `chain/l2/trade-lifecycle/semantic-model.json` as re-selected by `chain/l2/trade-lifecycle/review.json` after cycle 5 (use its model_sha256).
- **Exact active change authority:** the re-selected L2 model. What changed: `placed_at` now derives from the earliest held report across two anchored sources, `raw.trade_history` (per-status rows for historical-load trades; th_t_id, th_dts, th_st_id, batch_date) and `raw.trade_cdc`, with the history row preferred where it exists because report_order states it is the earliest held report; `first_seen_late` considers the earliest report across both sources (status from th_st_id or t_st_id); `owning_account_number` still comes from the Trade record's t_ca_id (history carries no account). The anchored table `raw.trade_history` exists in the fixture database with real Batch1 rows for trades 353232 and 372101.
- **Also changed (cycle 6, if selected before you run):** a frozen `owning_customer_effective_from`: the effective_from of the `governed.customer` statement for the customer named by the pinned account statement, with the latest effective_from at or before placed_at. Read containment then includes `governed.customer`; declare it. `inv.trade_placement_reference_frozen` covers it.
- **Stale artifact (re-project):** the trade model file. It derives from `sg.placement-moment`, whose fingerprint changed. Read containment now includes `raw.trade_history`; declare it in reads.
- **Hit artifacts (keep unless the model text changed their invariant):** the five audits; re-check `inv.trade_first_seen_late_matches_status_order` and `inv.trade_placement_reference_frozen` against the model's revised statements and adjust only if the predicate must change.
- **Expected on the fixture (for your own check):** trade 353232 placed_at 2017-01-12 14:55:11 (its earliest history row), first_seen_late false; trade 372101 placed_at 2017-04-10 19:28:01 (its history row equals its Batch1 snapshot time), first_seen_late false; both still pin account 428's 2012 statement; two-phase simulation still leaves ownership pinned while outcome changes.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** unchanged.
