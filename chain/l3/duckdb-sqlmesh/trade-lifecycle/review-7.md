# Review 7 (sketch reviewer, scoped): trade-lifecycle on duckdb-sqlmesh, L3 cycle 7 (verdict: revise; routed)

Faithful on the model, the gate and five audits; the DISTINCT fix faithful. Rejected: `inv.every_received_trade_persisted` (lost the held-only / held-first-report absence branch the native sibling carries, so 900005, 900007, 900008's absence rests on the simulation alone here), `inv.trade_order_type_frozen` (restated to every-anchored-report equality beyond the L2's first-encountered claim, without change authority, diverging from native). Recorded: inner joins on pins here versus LEFT JOINs on native make "no row" mean both authorized hold and broken pin on this engine, inside holes already open (pin-present, deletions). Routing: L2 cycle 16 addendum (an explicit invariant that a held-first trade has no row; decide order_type_frozen's shape; state the earliest-report rule when all history rows are held); L3 cycle 8 (restore the absence branch; align order_type_frozen with whatever the L2 decides; joins as before). Sketch and CE: no change needed this time.
