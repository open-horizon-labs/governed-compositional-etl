# Change contract, L3 cycle 5: trade-lifecycle on duckdb-sqlmesh (market orders are not first seen late)

- **Prior policy authority:** `chain/l2/trade-lifecycle/selected-model.json` at cycle 10, the model your accepted cycle compiled from.
- **Exact active change authority:** the re-selected L2 after cycle 11 (take model_sha256 from `chain/l2/trade-lifecycle/review.json`). Upstream cause: the business amended `L1.placement-moment` (an order sent straight to market has no pending stage and is not first seen late when its earliest report is submitted) and the anchors gained `trade_code_meanings.trade_type_codes`. Cache: `sg.placement-moment` stale; every other group hit. What changed in the L2: `logical.trade.first_seen_late`'s derivation (market orders' first lifecycle event is SBMT, limit orders' is PNDG), a new frozen attribute `order_type` handed off from `raw.trade_cdc.t_tt_id` at first encounter, `inv.trade_first_seen_late_matches_status_order` restated, and the group's coverage claim. Read the selected model for the exact texts.
- **Stale artifacts (re-project):** `models/trade.sql` (add order_type to `columns (...)` and the SELECT; add it to `frozen_columns`; the gate checks the header properties against the L2 roles), `audits/inv.trade_first_seen_late_matches_status_order.sql`. If the plan ignores the edit, delete `build/chain-duckdb-sqlmesh-trade-lifecycle/.cache`; the restatement of a frozen derivation re-materializes the model.
- **Hit artifacts (keep):** the other seven audits; their SQL is unchanged. Update `manifest.json` provenance and the entity artifact's `derived_from` with the new element ids.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** as before. Read containment unchanged (the new attribute comes from `raw.trade_cdc`, already a declared source). Do not decide what a market order reported PNDG means; the derivation only says which status is the first lifecycle event for each anchored order type; an unanchored t_tt_id is the model's review trigger, and the audit should surface it as a violation rather than default it.
- **Acceptance:** `chain_l3.py check duckdb-sqlmesh trade-lifecycle` ok; `run` ok, eight audits zero (fixture trades 353232 and 372101 are limit orders: 353232 stays not late, 372101 stays not late); `twophase` both ok; `mutate` zero unprotected (order_type is frozen, so the harness will null and swap it; the restated audit must catch both); then `simulate duckdb-sqlmesh trade-lifecycle --ce counterexamples/proposed/ce-market-order-first-seen-submitted-v1.json`: expect no audit fired and, in the samples, trade 900002 (TMB, first report SBMT) first_seen_late false and trade 900003 (TLB, first report SBMT) first_seen_late true. Report the two rows.
