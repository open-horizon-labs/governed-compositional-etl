# Change contract, L3 cycle 7: trade-lifecycle on duckdb-sqlmesh (reports held as wholes; a held first report is unclaimed)

- **Exact active change authority:** the re-selected L2 (model_sha256 from `chain/l2/trade-lifecycle/review.json`), compiled under `L1.unknown-codes` as now stated (a report is held as a whole: one unknown code holds the record) and the new hole `L1.hole.held-first-report-placement` (a trade whose earliest report is held is unclaimed: no row, reported). Read every restated element: the eleven handoffs' parallel_assumption, placed_at, `inv.trade_placed_at_matches_earliest_report`, `inv.trade_outcome_updates_in_place`, `inv.every_received_trade_persisted`, `inv.trade_first_seen_late_defined_or_held`. Also `review-6.md` in this directory (native) or the coordinator's routing of it (sqlmesh).
- **Stale artifacts:** the entity SQL: every CTE that reads raw.trade_cdc (first report, outcome selection, pins) reads only rows whose cdc_flag, t_st_id and t_tt_id are all anchored, with null-sensitive tests (`IS NULL OR NOT IN`), and raw.trade_history rows only where th_st_id is anchored; a trade whose earliest report is held has no row; a held later report is not a later report. Audits: `inv.unknown_codes_held` null-sensitive; `inv.trade_outcome_updates_in_place`, `inv.trade_placed_at_matches_earliest_report`, `inv.trade_placement_reference_frozen`, `inv.trade_owning_account_frozen` restated over anchored rows only (a held report is not evidence); `inv.every_received_trade_persisted` and `inv.trade_first_seen_late_defined_or_held` as the model now states them (a trade with a held first report is unclaimed). Leave `group_fingerprints` alone; the coordinator stamps.
- **Acceptance:** check ok; run 12 audits zero; twophase; mutate zero unprotected; `simulate duckdb-sqlmesh trade-lifecycle --ce counterexamples/proposed/ce-held-for-review-trades-v1.json`: inv.unknown_codes_held four rows (900005, 900006's later report, 900007, 900008's first report) and inv.trade_market_order_seen_pending_held one row (900004); 900006 stands at PNDG; 900005, 900007 and 900008 have no row; the market-order document stays silent with 900002 not late and 900003 late. On SQLMesh the harness reports the first blocking audit per plan; confirm the second by querying the simulation database or by running the audit SQL directly, and say which you did.
