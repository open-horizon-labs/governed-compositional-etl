# Change contract, L3 cycle 9: trade-lifecycle on duckdb-sqlmesh (the pending-market-order question is answered)

- **Prior policy authority:** the L2 selection your accepted cycle compiled from.
- **Exact active change authority:** the re-selected L2 (take model_sha256 from `chain/l2/trade-lifecycle/review.json`), compiled under an amended `L1.placement-moment`: the brokerage records a market order as pending on receipt, so that pending record is the order's own first lifecycle event and such a trade is **not** first seen late. `L1.hole.market-order-seen-pending` is closed and gone. Cache: `sg.placement-moment` stale; nothing else in any job.
- **Stale artifacts:** `models/trade.sql` (SELECT and the model's audits list); AUDIT files under `audits/`.
  - `first_seen_late`: a market order is late only when its earliest anchored report's status is later than SBMT (CMPT or CNCL); PNDG and SBMT are both opening events for a market order. Limit orders unchanged. No rank arithmetic may reach a held case; the only remaining held reason is an unknown code on the first-encountered report, and such a trade has no row.
  - Delete `audits/inv.trade_market_order_seen_pending_held.sql` and its registration: the invariant no longer exists, and the gate rejects an audit naming an unselected invariant.
  - Restate `inv.trade_first_seen_late_matches_status_order` (the market-pending case is now claimed) and `inv.trade_first_seen_late_defined_or_held` (the held set shrinks) as the model now states them.
- **Hit artifacts (keep):** everything bounded by `L1.hole.held-first-report-placement`, which stays open: the held-first-report gate, `inv.trade_held_first_report_unclaimed`, `inv.unknown_codes_held`, the frozen and outcome audits. Update manifest provenance; leave `group_fingerprints` to the coordinator's stamp.
- **Forbidden:** deciding anything about a trade whose earliest report is held; that hole is still open.
- **Acceptance:** `chain_l3.py check duckdb-sqlmesh trade-lifecycle` ok with 12 audits; `run` ok, 12 at zero; `twophase` ok; `mutate` zero unprotected; `simulate duckdb-sqlmesh trade-lifecycle --ce counterexamples/proposed/ce-held-for-review-trades-v1.json` fires exactly `inv.unknown_codes_held` with five rows, and trade 900004 persists with `first_seen_late` false while 900006 stands at PNDG and 900005, 900007, 900008, 900009 have no row; `simulate ... --ce counterexamples/proposed/ce-market-order-first-seen-submitted-v1.json` stays silent with 900002 not late and 900003 late. Report the rows.
