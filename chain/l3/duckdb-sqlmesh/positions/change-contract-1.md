# Change contract, L3 cycle 1: positions on duckdb-sqlmesh

- **Prior policy authority:** `chain/l2/positions/semantic-model.json` restricted to `chain/l2/positions/review.json` `selected_element_ids` (verdict must be pass; use its model_sha256). Upstream entities `governed.trade` (and, if the model's handoffs name them, `governed.account`, `governed.customer`) exist from the earlier jobs' projections on this target; consume only what the model's handoffs name. L1.holdings-follow-trade forbids resolving a holding's owner against the account or customer: the gate's read containment will reject a join to `governed.account` or `governed.customer` from the holding-change artifact.
- **Exact active change authority:** none. Initial compilation.
- **Stable projection contracts:** `chain/anchors/DEVELOPER-CONTRACT-L2-L3.md` (including the SQLMesh layout section where applicable); profile `chain/profiles/duckdb-sqlmesh.json`. Output under `chain/l3/duckdb-sqlmesh/positions/`: `manifest.json`, one artifact per selected entity (`holding_change`, `account_position`, `customer_position`, named as the model names them), `audits/<invariant id>.sql` per selected deterministic invariant.
- **Write surface:** the holding-change entity is incremental_by_identity on (original trade, current trade): per the profile, a native MERGE with the matched update limited to the mutable quantity attributes (duckdb-native), or `kind CUSTOM governed_merge` with a comma-separated unique_key (duckdb-sqlmesh; the materialization accepts composite keys). Frozen ownership attributes are copied from `governed.trade` at first encounter and never updated. The two position entities are aggregates: rebuild.
- **Simulation:** `chain_l3.py run duckdb-sqlmesh positions` executes the upstream projections first. Expected on the fixture: the 353232 open (0 to 5225) and its close through 372101 (5225 to 0) both carry the ownership recorded on their causing trade (353232's own for the open, 372101's for the close); both trades pin account 428's 2012 statement and customer 238's 2008 statement, so the account position and the customer position for those statements net to zero and no statement is negative. The two-phase run must keep the holding change's ownership pinned while the account gains its labeled 2017 statement.
- **Forbidden shortcuts / conflict protocol:** unchanged.
