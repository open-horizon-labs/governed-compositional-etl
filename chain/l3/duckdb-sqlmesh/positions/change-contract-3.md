# Change contract, L3 cycle 3: positions on duckdb-sqlmesh

- **Prior policy authority:** `chain/l2/positions/selected-model.json` at cycle 5, the model your cycle 2 compiled from.
- **Exact active change authority:** the re-selected L2 (take model_sha256 from `chain/l2/positions/review.json`). What changed: one new deterministic invariant, `inv.eligible_holding_report_persisted` (every raw.holding_history pair none of whose reports carries cdc_flag D, whose current trade resolves to a logical.trade row with all four ownership values non-null, has exactly one holding_change row; a D-touched pair is not claimed); text in `inv.holding_change_current_trade_known.parallel_assumption`, `sg.holding-attribution.coverage_claim` and `hole.batch_identity`. No type, entity, derivation, handoff or selector changed. Also active: `review-2.md` in this directory.
- **Stale artifacts (add or fix):**
  - New audit `audits/inv.eligible_holding_report_persisted.sql` (AUDIT files over `@this_model`, dotted names quoted in the models' audits lists): the expected set is the non-D pairs from `raw.holding_history` (D test per pair with `BOOL_AND(cdc_flag IS DISTINCT FROM 'D')`, so null-flag historical rows stay in scope and a pair with any D report drops out entirely) joined to `governed.trade` on the current trade with all four ownership values non-null; report pairs with zero or more than one holding_change row. Register it in the manifest (and, on SQLMesh, in holding_change's audits list).
  - Rewrite `audits/inv.holding_quantity_updates_in_place.sql` (rejected in `review-2.md`) to the tolerant form: a persisted pair is a violation only when NO undominated candidate report matches its quantities (EXISTS undominated AND NOT EXISTS matching), with clause 2 (persisted pair with no eligible report) kept separate; use the model's own candidate filter (`cdc_flag IS NULL OR cdc_flag IN ('I','U')`) or state why it differs; make the comment true (the tie is hole.batch_identity's case and this audit accepts any undominated candidate). Make the `cdc_dsn` comparison null-safe (COALESCE) so a same-batch incremental report with a null cdc_dsn does not tie spuriously.
  - In both sum audits add the cardinality half the invariants state (a key with more than one position row is a violation) and use IS NOT DISTINCT FROM in the key joins as the native sibling does, so both engines read the same.
- **Hit artifacts (keep):** models and the other audits unchanged. Update manifest provenance. Keep the filed question.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** as before. Read containment for the new audit: `governed.holding_change`, `governed.trade`, `raw.holding_history`.
- **Acceptance:** `chain_l3.py check duckdb-sqlmesh positions` question (the filed question) with zero problems and 13 audits; `run` ok, 13 at zero; `twophase` both ok; `mutate` zero unprotected; then `simulate duckdb-sqlmesh positions --ce counterexamples/proposed/ce-holding-historical-report-tie-v1.json` must be silent (either tied candidate accepted; the tie stays the hole's case) and `simulate duckdb-sqlmesh positions --ce counterexamples/proposed/ce-trade-before-account-statement-v1.json` is reported as observed (that document carries no holding report, so positions audits should be silent while the upstream trade audit fires). Report briefly.
