# Change contract, L3 cycle 4: positions on duckdb-native (unknown codes are held, never interpreted)

- **Prior policy authority:** the previous selection your accepted cycle compiled from.
- **Exact active change authority:** the re-selected L2 (take model_sha256 from `chain/l2/positions/review.json`), compiled under the Sketch's new clause `L1.unknown-codes` (a fixed vocabulary; an unknown code is held for review, never interpreted, defaulted or dropped; every job reports it naming the record and the code). Read the selected model for the new group `sg.unknown-codes` and its invariant, and every element whose parallel_assumption now says an unknown value yields no derived fact.
- **Stale artifacts:** Add the audit for `inv.unknown_codes_held` (raw.holding_history rows whose cdc_flag is outside I, U, D, null). The candidate set stays the anchored codes; an unknown flag is reported by this audit rather than dropped silently or admitted. Retire the cdc_flag-vocabulary question from `questions_for_authority` (the clause answers it); keep the D-only-pair question. Then `simulate <target> positions --ce counterexamples/proposed/ce-holding-unanchored-cdc-flag-v1.json` must fire `inv.unknown_codes_held` (and may still fire the converse audit) naming the row with flag X.
- **Forbidden:** an ELSE branch or default for an unknown code; dropping a row because of an unknown code; deciding what a held record means afterwards.
- **Acceptance:** `chain_l3.py check duckdb-native positions` ok or question with zero problems and one audit per selected deterministic invariant; `run` ok with every audit at zero on the fixture; `twophase` ok; `mutate` zero unprotected; the simulations named above. Report briefly.
