# Change contract, L3 cycle 2: positions on duckdb-native

- **Prior policy authority:** `chain/l2/positions/selected-model.json` at cycle 3 (review model_sha256 daa522342a062dc26c3e368fb639edb4b791db1ffdf662343fb060d9e9e784e5), the model your cycle 1 compiled from.
- **Exact active change authority:** the re-selected L2 (`chain/l2/positions/selected-model.json` and `review.json`; take the current model_sha256 from review.json). What changed since your cycle 1: six new deterministic invariants and feedback lists naming them; two coverage claims and the deletions hole gained a sentence. No type, entity, attribute derivation, handoff or selector changed. Also active: your projection review, `review-1.md` in this directory, which rejected the artifacts named there.
  - `inv.holding_quantity_change_is_difference` (logical.holding_change)
  - `inv.account_position_is_sum_of_changes` (logical.account_position), `inv.customer_position_is_sum_of_changes` (logical.customer_position)
  - `inv.holding_change_ownership_present` (logical.holding_change)
  - `inv.holding_change_current_trade_known` (logical.holding_change)
  - `inv.holding_quantities_present` (logical.holding_change)
- **Stale artifacts (re-project or add):**
  - Replace the INNER JOIN on trade existence by the model's own rule: join `governed.trade` and keep a report only when all four ownership values are non-null (`inv.holding_change_ownership_present`); a report whose current trade row exists with a null pin is excluded, a report whose current trade has no row at all is excluded from the entity but now fires through the audit for `inv.holding_change_current_trade_known`. Do not add NOT NULL constraints to the CREATE TABLE: a constraint would abort the MERGE before any audit ran, turning a reportable violation into a load failure; the presence audits are the mechanism.
  - Six new audit files, one per invariant above, registered in `manifest.json` (and on this target's layout as the Developer contract describes: `holding_change.sql`, `account_position.sql`, `customer_position.sql`, `audits/<inv>.sql`). Each recomputes from the anchored source or the job's own entities and compares with `IS DISTINCT FROM`. The current-trade-known audit reads `raw.holding_history` (reports whose cdc_flag IS DISTINCT FROM 'D': historical rows carry a null cdc_flag and are in scope; `<> 'D'` would silently drop them) and `governed.trade`, and returns the pairs whose hh_t_id has no trade row. The sum audits recompute each position from `governed.holding_change` and also return keys present among holding changes with no position row, and position rows with no holding changes.
  - Your two not-negative audits already test `OR net_quantity IS NULL`; keep them.
  - `audits/inv.holding_quantity_updates_in_place.sql`: cover the invariant's second clause too: a persisted (original_trade_number, current_trade_number) pair with no surviving I, U or historical report is a violation. Where possible restate the latest-report selection independently of the projection's CTE rather than copying it.
  - Fix the header of `holding_change.sql` lines 3-4: Batch1 holding rows are NOT loader-normalized to I/0; say what the later comment block says.
  - Add an artifact `note` to `manifest.json` recording that `inv.holding_change_not_constructed` is deterministic false and has no audit by design (the sqlmesh sibling has one).
  - File one question in `manifest.json` `questions_for_authority` for the case the review found decided in SQL: a pair whose only reports are D-flagged currently has no holding_change row; `hole.deletions` reserves row existence. State the question precisely; do not change the behavior. Also note, in a comment, that several historical reports of one pair would tie in the ordering (hole.batch_identity).
- **Hit artifacts (keep):** the two position models and the four audits the review did not reject; behavior unchanged. Update `manifest.json` provenance (`derived_from_model.review_sha256`) and add the new invariant ids where the manifest places invariant coverage.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** as in cycle 1 and the Developer contract. Read containment unchanged: holding_change reads only `raw.holding_history` and `governed.trade`; positions read only `governed.holding_change`; audits read their entity, the job's other entities, `governed.trade`, and `raw.holding_history`.
- **Acceptance:** `chain_l3.py check duckdb-native positions` ok or question (the filed question is expected); `run duckdb-native positions` ok with twelve audits at zero; `twophase duckdb-native positions` both phases ok; `mutate duckdb-native positions` zero unprotected (the harness now perturbs quantity_change and nulls non-nullable attributes; the difference and presence audits must catch those); `compare duckdb-native positions --against duckdb-sqlmesh` identical (the coordinator runs compare). Report the mutate summary and which audit catches each mutation class.
