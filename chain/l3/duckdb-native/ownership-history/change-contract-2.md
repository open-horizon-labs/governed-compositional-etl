# Change contract, L3 cycle 2: ownership-history on duckdb-native

- **Prior policy authority:** `chain/l2/ownership-history/semantic-model.json` as selected by `chain/l2/ownership-history/review.json` (model_sha256 bf7b63a4dfa6d73b1616bece8cf6dac5c9df6d47dfaac098c7532ad5d93e4cd2). Your cycle-1 manifest cites an older review sha; update it.
- **Exact active change authority:** the re-selected L2 model. What changed since your cycle 1: `logical.customer.tier` and `logical.account.tax_treatment` now declare `derivation` carried_forward_from_previous_statement (the fact stands as last stated when the producing action omits it: INACT omits c_tier, CLOSEACCT omits ca_tax_st); the two content invariants are now unconditional; justification text changed elsewhere without changing behavior.
- **Stale artifacts (re-project):** `customer.sql`, `account.sql` (they derive from `sg.statement-content` and `sg.owner-standing`). Implement the carry-forward for tier and tax_treatment the way you already did for the owner: last non-null earlier statement of the same identity, over the effective_from-ordered feed. Clear your two filed questions; they are answered by the model.
- **Hit artifacts (keep):** all nine audits that passed; their SQL is unchanged. Re-check `audits/inv.customer_statement_has_content.sql` and `audits/inv.account_statement_has_content.sql`: their header comments quote the old hedged invariant text; update the comments to the current statement text; the predicate (no null content on any statement) should now hold.
- **Stable projection contracts:** unchanged. `manifest.json` `derived_from` lists must name the current element ids (add `logical.customer.tier` and `logical.account.tax_treatment` derivation coverage where relevant).
- **Forbidden shortcuts / conflict protocol:** unchanged.
- **Acceptance:** `chain_l3.py check duckdb-native ownership-history` ok and `chain_l3.py run duckdb-native ownership-history` ok with all 11 audits at zero violations; report the sample rows for customer 238 and account 428 so the reviewer can see the carried tier and tax treatment.
