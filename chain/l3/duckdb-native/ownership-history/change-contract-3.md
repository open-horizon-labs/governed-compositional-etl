# Change contract, L3 cycle 3: ownership-history on duckdb-native

- **Prior policy authority:** the L2 selection your accepted cycle compiled from (review-1/2 lineage; manifest review sha now stale).
- **Exact active change authority:** `chain/l2/ownership-history/selected-model.json` as selected by `chain/l2/ownership-history/review.json` (model_sha256 12961d03caa20257bd2a2980d08f688fc8985a80d435c776c803d42467a8dddf). What changed since your accepted cycle: five new deterministic invariants, and five attributes now list them in `feedback`. Nothing else in the model changed: no type, entity, attribute derivation, handoff, hole, or selector.
  - `inv.customer_status_matches_producing_source` (entity logical.customer)
  - `inv.customer_tier_matches_producing_source` (logical.customer)
  - `inv.account_status_matches_producing_source` (logical.account)
  - `inv.account_tax_treatment_matches_producing_source` (logical.account)
  - `inv.account_owner_matches_producing_source` (logical.account)
- **Why:** mutation testing on your accepted projection (`mutation-findings-1.md`) found three mutations no audit caught: swapping status between two customer or two account statements, and swapping tax_treatment. The L2 cycle that authorized these invariants was the route that finding took.
- **Stale artifacts (add):** one audit file per invariant above, named `audits/<invariant id>.sql`, registered in `manifest.json` with the entity and invariant id, following the layout your other audits use. Each recomputes the expected value from the anchored source and compares with `IS DISTINCT FROM`, so a null projected value is a violation. Audit reads: the entity itself, the job's other entity, and the entity's declared sources (`raw.customer_mgmt_action`, `ce.account_changes`); nothing else.
  - Status: join the producing historical action and map action_type by the anchored meaning (customer: NEW, UPDCUST active; INACT inactive; account: NEW, ADDACCT, UPDACCT open; CLOSEACCT closed), using the status_codes vocabulary your projection already emits. For account statements with provenance controlled_counterexample, expected status is the ce row's status_id.
  - Tier and tax_treatment: where the producing row carries the field (customer NEW, UPDCUST carry c_tier; account NEW, ADDACCT, UPDACCT carry ca_tax_st; every ce row carries tax_status_id) expected equals the row's value; where the action omits it (INACT, CLOSEACCT) expected equals the value of the same identity's immediately preceding statement, recomputed from the anchored source rather than read back from the projected column.
  - Owner: where a historical action produced the statement, expected equals the action's c_id; for a ce-produced statement (no owner field), expected equals the immediately preceding statement's owner.
  - Do not make an audit circular. Customer audits join the producing action on (customer_number, effective_from) = (c_id, action_ts). Account audits join on (account_number, effective_from) = (ca_id, action_ts) for historical rows and (account_id, change_ts or the timestamp column your projection used) for ce rows. Never derive the expected value from the column being checked.
- **Hit artifacts (keep):** your entity SQL and the eleven existing audits are unchanged; their behavior is not in question. Update `manifest.json`: review_sha256 to the sha above, and add the five audits. Do not edit derived_from lists for entity artifacts except to add the new invariant ids where the manifest schema places invariant coverage.
- **Stable projection contracts:** unchanged. Read containment, write surface, is_current rule, and quoting conventions as in your accepted cycle.
- **Forbidden shortcuts / conflict protocol:** as in the Developer contract. If a statement's producing row cannot be identified by the joins above (for example two actions at one timestamp for one identity), file a question in the manifest rather than guessing; report it and stop.
- **Acceptance:** `chain_l3.py check duckdb-native ownership-history` ok; `chain_l3.py run duckdb-native ownership-history` ok with all sixteen audits at zero violations; `chain_l3.py mutate duckdb-native ownership-history` reports zero unprotected. Append the mutate summary as `mutation-findings-2.md` (which mutations now fire, and which audit caught each). Report which audit catches each of the three previously unprotected mutations.
