# Review 7 (sketch reviewer, scoped): trade-lifecycle on duckdb-native, L3 cycle 7 (verdict: revise; routed)

Closed: latest_outcome and the inherited audits read anchored rows; the earliest-report gate excludes a held-first trade whole. Open: three audits still read a held history row's status through COALESCE; inv.trade_order_type_frozen checks only the first report here while the SQLMesh sibling checks every anchored report; the projection sources ownership and order type from a later CDC report when the earliest CDC report is held under an anchored earliest history row, a case no layer above decides; whether a held report keeps its place in the report order was assumed.

Routing applied: Sketch (a held report keeps its place in the report order, nothing else may be read; the hole widened to every placement-fixing fact, with such trades held until answered); CE (trade 900009 added); L2 cycle 16 (restate inv.trade_owning_account_frozen, inv.trade_placement_reference_frozen, inv.trade_order_type_frozen to exclude held reports; carry the widened hole); L3 cycle 8 (filter first_history_report to anchored th_st_id in the three audits; null-sensitive held test; order_type_frozen over every anchored report; hold 900009). Accepted as projected: every_received_trade_persisted, placed_at_matches_earliest_report, outcome_updates_in_place, unknown_codes_held.
