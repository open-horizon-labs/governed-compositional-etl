# Review 8 (sketch reviewer, scoped): trade-lifecycle on duckdb-native, L3 cycle 8 (verdict: revise, one audit)

Review 7's findings closed; the widened hold faithful and provably the same set as the SQLMesh gate; the absence audit exact. Rejected: `audits/inv.trade_order_type_frozen.sql`, whose every-report branch is not joined to governed.trade and so asserts agreement for trade_numbers the job does not claim (inside two open holes); its sibling owning-account audit is row-scoped. Statement drift: `inv.trade_first_seen_late_defined_or_held` keeps a dead unknown-code disjunct and a header claiming it. Routed: SQL cycle 9 (scope the branch to governed.trade; fix the header); L2 cycle 17 (one sentence: the frozen invariants quantify over trades the job claims a row for); CE expected text brought up to the widened hole (done).
