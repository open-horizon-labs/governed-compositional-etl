# Change contract, L3 cycle 4: trade-lifecycle on duckdb-native

- **Prior policy authority:** `chain/l2/trade-lifecycle/selected-model.json` at cycle 7, the model your accepted cycle compiled from.
- **Exact active change authority:** the re-selected L2 (`chain/l2/trade-lifecycle/selected-model.json`; take model_sha256 from `review.json`). What changed: two new deterministic invariants, `inv.trade_ownership_pin_present` and `inv.every_received_trade_persisted`; feedback lists and the four pin attributes' review_trigger texts name them; `hole.change_effective_time` gained one sentence. No type, entity, derivation, handoff or selector changed.
- **Why:** the two engine projections of the same L2 disagree about a trade whose account or customer pin does not resolve (no statement of the owning account at or before placed_at). Your \`trade.sql\` LEFT JOINs the two pins and persists a trade whose pin does not resolve with null values in four \`nullable: false\` attributes. Under the re-selected model that row violates \`inv.trade_ownership_pin_present\`. Keep the LEFT JOIN or switch to an inner join as you judge simplest; either way one of the two new audits must fire for such a trade, and the audits, not the join shape, are the contract. Add NOT NULL to the \`nullable: false\` columns in the CREATE TABLE if you keep the LEFT JOIN, so the projection itself refuses the row and the run reports it. See `chain/ce/proposed/ce.l2.trade-before-account-statement.md` for the constructed case; the fixture carries no such trade, so both audits pass at zero on it.
- **Stale artifacts (add):** `audits/inv.trade_ownership_pin_present.sql` (rows of governed.trade with any of the four pin columns null) and `audits/inv.every_received_trade_persisted.sql` (trade_numbers reported by raw.trade_cdc or raw.trade_history none of whose reports carries cdc_flag D, that have no governed.trade row, plus any trade_number with more than one row; write the D test as IS DISTINCT FROM so null flags on historical rows stay in scope). Register both in `manifest.json` (and on SQLMesh in the model's audits list, dotted names quoted). Reads: the entity, `raw.trade_cdc`, `raw.trade_history`.
- **Hit artifacts (keep):** the entity SQL (unless you add NOT NULL as above) and the six existing audits. Update `manifest.json` provenance.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** as before.
- **Acceptance:** `chain_l3.py check duckdb-native trade-lifecycle` ok; `run duckdb-native trade-lifecycle` ok with eight audits at zero; `twophase` both phases ok; `mutate` zero unprotected. Report briefly.
