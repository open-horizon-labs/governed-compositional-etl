# Review 8 (sketch reviewer, scoped): job positions (verdict: pass, one phrase)

`inv.unknown_codes_held` restates the clause exactly over cdc_flag (I, U, D, null for historical rows, anchored twice); feedback and parallel_assumption on exactly the five cdc_flag readers; gap none; the cdc_flag-vocabulary question retired honestly. One phrase in `sg.no-phantom-positions.coverage_claim` reads as if an unknown flag were no longer excluded from the candidate set; it is still excluded, the exclusion is no longer silent.
