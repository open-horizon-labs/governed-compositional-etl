# Change contract, cycle 6: L1 -> L2, job positions (what the cycle-2 projection reviews found)

- **Prior policy authority:** unchanged (`sketches/l1-brokerage-intent-v1.md`, the clauses for `job:positions`; `selected-model.json` at cycle 5).
- **Exact active change authority:** `chain/l3/duckdb-native/positions/review-2.md` (converse gap; engine-dependent outcome of `inv.holding_change_current_trade_known`) and `chain/l3/duckdb-sqlmesh/positions/review-2.md` (same engine dependence, recorded).
- **Authorized addition (deterministic invariant, disposition candidate, group sg.holding-shape, derived_from a subset of that group's parents, full fields, added to the feedback of the elements it checks):**
  1. `inv.eligible_holding_report_persisted` (logical.holding_change): every (original_trade_number, current_trade_number) pair reported by raw.holding_history, none of whose reports carries cdc_flag D, whose current trade resolves to a logical.trade row carrying all four ownership values non-null, has exactly one logical.holding_change row. This is the converse of `inv.holding_quantity_updates_in_place`'s second clause and restates the entity's grain together with the exclusion rule already in `sg.holding-attribution`: a report is kept exactly when its pin resolves. A pair with any D report is L1.hole.deletions' case and is not claimed.
- **Authorized text corrections:**
  2. `inv.holding_change_current_trade_known.parallel_assumption`: record that "resolves to a logical.trade row" and "resolves to a fully pinned row" coincide whenever trade-lifecycle's own invariants (`inv.every_received_trade_persisted`, `inv.trade_ownership_pin_present`) hold, and that an engine projection which drops an unpinnable trade upstream will make this invariant fire for the unresolved-pin case too; the two engines differ only in a state trade-lifecycle already reports. Same sentence, shorter, in `sg.holding-attribution.coverage_claim` where it distinguishes the two cases.
  3. `hole.batch_identity` (or the hole text that bounds report ordering): name that when several historical reports of one pair tie under report_order, which candidate's quantities the pair carries is undecided here, and that a projection adopting any undominated candidate is one behavior consistent with the hole, not a resolution.
- **Current rules that must be preserved:** all clauses; every cycle-5 element stands unchanged except as enumerated. No type, entity, derivation, handoff, selector or disposition moves.
- **Conflict protocol reminder:** if an item needs text this contract does not name, file exactly one question in `questions_for_authority`, omit the element, and return.
- **Acceptance:** `.venv/bin/python scripts/chain_l2.py check positions` ok with zero problems; diff against `selected-model.json` confined to the elements enumerated here.
