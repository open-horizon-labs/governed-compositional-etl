# Review 5 (coordinator, bookkeeping): job positions (verdict: pass)

Scope: cycle 5 under `change-contract-5.md`, the six corrections review 4 prescribed. Element-level diff against `selected-model.json` (cycle 3): six invariants added, all now in the groups review 4 named (the two quantity invariants in `sg.no-phantom-positions`, citing L1.no-phantom-positions); feedback changed on exactly the attributes, handoffs and two types those invariants check, including the `hh_t_id` handoff review 4 found missing; `sg.holding-shape` back to its selected members and coverage claim with `gap` naming L1.hole.deletions; `sg.holding-attribution` and `sg.no-phantom-positions` members and coverage claims; `hole.deletions` question reworded without projection language and `blocks` naming both bounded groups. No type, entity, derivation, handoff, selector or disposition moved. Gate ok, zero problems, zero questions.

Reviewer: coordinator acting as sketch reviewer for a cycle whose every line the prior review prescribed. Review 4's findings stand as the record; the gate and this diff are the deterministic protection.

Rejected element ids: none. Selected: cycles 4 and 5 together.
