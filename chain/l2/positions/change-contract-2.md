# Change contract, cycle 2: L1 -> L2, job positions (after trade-lifecycle cycle 6 is selected)

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:positions`.
- **Exact active change authority:** the re-selected trade-lifecycle model, whose frozen ownership reference now carries `owning_customer_effective_from`; the adjudication in `.oh/metis/issue-8-statement-content-assumed.md`; the corrections `review-1.md` authorizes.
- **Authorized corrections:** copy `owning_customer_effective_from` onto the holding change (frozen) and key `logical.customer_position` on (customer_number, customer_effective_from), restating sg.no-phantom-positions' coverage claim to cover both halves of L1.no-phantom-positions; add L1.hole.change-effective-time to sg.no-phantom-positions' gap and list the group in that hole's blocks; carry L1.hole.owner-change-reversions-account blocking sg.holding-attribution; restate inv.holding_change_not_constructed as the two checkable facts (no ce.* handoff into the entity; the copied pair keeps the pinned statement's provenance reachable) and align its deterministic flag with the upstream convention; add a deterministic invariant for quantity update-in-place with D excluded provisionally (parallel to inv.trade_outcome_updates_in_place); cite report_order's `applies_to` (now stated for holding-change reports) for latest_change and first_encounter_only; declare the aggregate keys as handoffs from logical.holding_change with a group-key selector rather than computed.
- **Current rules that must be preserved:** all clauses; cycle-1 elements stand unless corrected here.
- **Explicit holes that must remain open:** unchanged; securities and brokers stays open.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** unchanged.
