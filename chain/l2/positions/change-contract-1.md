# Change contract, cycle 1: L1 -> L2, job positions

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:positions` (L1.identity, L1.attribution-at-placement, L1.holdings-follow-trade, L1.no-phantom-positions, L1.constructed-scenarios) and its feedback line.
- **Exact active change authority:** none. Initial compilation.
- **Upstream jobs you may consume:** `ownership-history` and `trade-lifecycle`, through their selected entities (read each job's `review.json` for `selected_element_ids`). A holding change belongs to whoever owned the trade that caused it, as that trade's ownership was recorded: consume `logical.trade`'s frozen ownership reference (owning_account_number, owning_account_effective_from, owning_customer_number) through the current trade of the holding change. Never resolve a holding's owner against `logical.account` directly; that is re-attribution, which L1.holdings-follow-trade forbids.
- **Approved outputs covered by that authority:** none yet.
- **Current rules that must be preserved:** every clause the job lists. A holding change is identified by its original trade and its current trade; its attribution is a copy of the current trade's frozen ownership and never changes; a position per account statement (and per customer statement) is the sum of holding changes attributed to it; no statement's position goes below zero (L1.no-phantom-positions is the end-to-end invariant of the whole chain).
- **Explicit holes that must remain open:** L1.hole.securities-and-brokers (positions are per account and customer only; nothing per security), L1.hole.deletions, L1.hole.batch-identity, L1.hole.change-effective-time, L1.hole.trade-timestamps as they touch this job.
- **Stable projection contracts:** `chain/anchors/semantic-model-v2.schema.json`, `chain/anchors/L2-FORMAT.md`, `chain/anchors/sources-v1.json` (holding changes arrive as `raw.holding_history`: original trade, current trade, before and after quantity, change flag and sequence for incremental rows). Output `chain/l2/positions/semantic-model.json` only. `upstream_jobs: ["ownership-history", "trade-lifecycle"]`.
- **Forbidden shortcuts:** as in `chain/anchors/DEVELOPER-CONTRACT-L1-L2.md`; additionally: no re-attribution through the account's current statement; no position per security or broker; no invented meaning for the holding change flag beyond the anchored cdc_flag meanings.
- **Conflict protocol:** one precise question in `questions_for_authority`, omit the affected elements, return.
