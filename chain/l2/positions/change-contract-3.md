# Change contract, cycle 3: L1 -> L2, job positions (text after the customer statement key)

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:positions`. Unchanged.
- **Exact active change authority:** none new. Text corrections authorized by `review-2.md`.
- **Authorized corrections:** rewrite the three customer-side texts to the statement key, mirroring the account twins: the `quantity_change -> logical.customer_position.net_quantity` handoff's necessity and parallel assumption; `logical.customer_position.net_quantity.necessity`; `inv.customer_position_not_negative.parallel_assumption` (a negative sum means a holding was opened under one customer statement and closed under another). Restate `inv.holding_change_not_constructed` and `sg.constructed-scenarios.coverage_claim` as three facts: no ce.* source is anchored for holding changes; the copied account pair keeps the pinned account statement's provenance reachable per the upstream invariant; no constructed customer source is anchored, so no customer-side provenance exists to carry. Add the key-match invariants to the four aggregate key attributes' feedback; replace "group_key conversion" with "computed_within_entity group key" (or restore the handoff form now that the gate allows it); name all four frozen values in `inv.holding_change_ownership_frozen.parallel_assumption`; list owning_customer_effective_from in `hole.trade_timestamps`; make `hole.owner_change_reversions_account` either block sg.no-phantom-positions (and appear in its gap) or drop its propagation sentence; drop ownership-history from upstream_jobs if no handoff draws from it.
- **Current rules that must be preserved:** all clauses; cycle-2 elements stand; no element other than those listed may change.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** unchanged.
