# Sketch review 2: job positions (verdict: fail, text)

Reviewer: Opus judge. Structural corrections landed: customer position keyed on (customer_number, customer_effective_from); holding change copies all four frozen values with first_encounter_only and never touches account or customer; both nonnegativity invariants deterministic over statement pairs; update-in-place invariant with D deferred; report_order applies_to cited accurately; blocks and gaps agree. The computed aggregate keys with equality invariants are an acceptable fallback (handoff form recommended, not required).

Defects: three customer-side texts still describe grouping by customer number (the net_quantity handoff's necessity and parallel assumption, net_quantity's necessity, and inv.customer_position_not_negative's parallel assumption, which states the forbidden cross-statement netting as its own credibility condition); inv.holding_change_not_constructed and the constructed-scenarios claim assert customer-pair provenance reachability that upstream does not support (logical.customer carries no provenance; the upstream invariant covers the account pair only).

Notes: add the key-match invariants to the four key attributes' feedback; fix the dangling "group_key conversion" phrase; inv.holding_change_ownership_frozen's parallel assumption should name all four values; hole.trade_timestamps should list owning_customer_effective_from; hole.owner_change_reversions_account either blocks sg.no-phantom-positions too or drops its propagation sentence; upstream_jobs may drop ownership-history since no handoff draws from it.
