# Sketch review 7 (scoped): job ownership-history (verdict: fail, two text elements)

Reviewer: Opus judge, scoped to cycle-7 elements.

Addressed: coverage claim per action and field; invariant assumptions and statements unconditional; carry-forward rules on tier and tax_treatment rest on L1.omitted-facts-stand with predecessor guarantees and the CDC-deferral bound.

Still open: `logical.account.owning_customer_number` gained the clause in derived_from but its rule and necessity still derive the carry from identity and history ("an unstated fact therefore continues ..."); `sg.owner-standing.coverage_claim` does the same. The citations are decoration on these two elements. Cause: change-contract-7 restricted owner edits to the citation; a contract error, corrected in contract-8.

Anchor leak (coordinator's): L2-FORMAT stated the owner carry-forward as a rule; recast as conditional on the clause.

Observation, not defect: predecessor existence rests on the anchored code meanings guaranteeing the customer or account existed, not that the extract holds its earlier statement; a truncated extract projects a null caught only by the content invariants and review triggers.

Carry to the next L3 cycle: the two L3 content audits' header comments quote the pre-cycle-7 hedged invariant text.
