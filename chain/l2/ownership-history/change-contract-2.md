# Change contract, cycle 2: L1 -> L2, job ownership-history

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:ownership-history`.
- **Exact active change authority:** approved CE `chain/ce/accepted/ce.l1.statement-content.md` and its clause `L1.statement-content` (assumed, pending business confirmation). Anchor change under the data-architect hat: mutation role `per_statement` added to `chain/anchors/semantic-model-v2.schema.json` and `chain/anchors/L2-FORMAT.md`.
- **Approved outputs and checked fields covered by that authority:** customer statements carry active/inactive status and tier; account statements carry open/closed status, tax treatment, and owning customer. Nothing else about standing is authorized.
- **Authorized corrections under the existing clauses (projection defects from `review-1.md`):** defer the CDC handoffs under L1.hole.change-effective-time and list every blocked group in the hole's `blocks`; use `per_statement` for versioned statement values instead of `mutable`; remove decorative citations (an element's derived_from must be a subset of its group's parent clauses; add the clause to the group or move the member); group gaps are `none` or an L1 hole id; add or explicitly dismiss L1.hole.closed-account-activity and L1.hole.batch-identity in the cycle report; anchor limitations go in `questions_for_authority`, not in gaps.
- **Current rules that must be preserved:** every clause the job lists, exactly as written, including the new clause.
- **Explicit holes that must remain open:** L1.hole.owner-change-reversions-account (do not derive account re-versioning on customer change), L1.hole.change-effective-time (do not date CDC rows at batch date), L1.hole.deletions, and any other L1 hole that touches the job.
- **Retained behavior that must not regress:** same-moment lookup through the owning customer reference (`inv.account_asof_carries_customer_asof`); constructed-scenario labels ride only on `ce.`-sourced rows.
- **Stable projection contracts:** `chain/anchors/semantic-model-v2.schema.json`, `chain/anchors/L2-FORMAT.md`, `chain/anchors/sources-v1.json`. Output `chain/l2/ownership-history/semantic-model.json` only.
- **Forbidden shortcuts:** as in `chain/anchors/DEVELOPER-CONTRACT-L1-L2.md`. In particular: the standing facts come from `L1.statement-content`, not from the anchored field names; the anchors only tell you which source fields exist to hand off from.
- **Conflict protocol:** leave all files unchanged except your model, put exactly one precise question in `questions_for_authority`, omit the affected elements, return.
