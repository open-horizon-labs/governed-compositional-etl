# Review 11 (sketch reviewer, scoped): job ownership-history (verdict: reject, partial)

Accepted: the `raw.customer_mgmt_action.action_type` half of `inv.unknown_codes_held` (six anchored codes exact), the group, the four status-handoff additions including the two deferred CDC fields against status_codes. Rejected: the `ce.account_changes.status_id` half. L1.unknown-codes presupposes a vocabulary; where none is anchored the format says file a question, not report every row of a labeled constructed source as a violation while the same handoff and `inv.account_status_matches_producing_source` interpret the field. The live proposal `ce.k.constructed-status-vocabulary` is the question. Also: re-escaped dashes and a dropped trailing newline outside the enumerated elements.

Coordinator disposition: cycle 12 removes the ce half, files the one question citing the CE, keeps blocks/gap with one line of justification, restores byte hygiene.
