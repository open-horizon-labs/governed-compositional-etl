# Sketch review 8 (scoped): job ownership-history (verdict: pass)

Reviewer: Opus judge. Leaf-by-leaf comparison against the last commit shows exactly three changed values, all authorized by contract-8. The owner carry-forward now rests on L1.omitted-facts-stand with identity and history demoted to defining the immediately preceding statement and the predecessor guarantee wired to inv.constructed_account_change_refers_to_known_account. The L2-FORMAT leak is recast. Review-7 items all addressed.

Non-blocking notes for the coordinator: L2-FORMAT's unconditioned "the current flag follows from there being no later statement" restates L1.current-version (conditioned now); sg.owner-standing leans on an invariant that is a member of sg.constructed-scenarios and is not in the owner attribute's feedback (legible by id; left as is); the manifest's sg.owner-standing entry is stale and needs re-baselining (done).
