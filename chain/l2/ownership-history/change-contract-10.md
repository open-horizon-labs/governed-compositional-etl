# Change contract, cycle 10: L1 -> L2, job ownership-history (value-correctness invariants for interpreted handoffs)

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:ownership-history`. Unchanged.
- **Exact active change authority:** none at L1. Feedback strengthening authorized by the audit mutation findings (`chain/l3/duckdb-native/ownership-history/mutation-findings-1.md`): swapping a statement's status or tax treatment for another statement's value fired no audit, because no deterministic invariant states that an interpreted or copied value equals what its handoff says.
- **Authorized correction:** add deterministic invariants that restate the value each candidate handoff supplies, so an L3 audit can recompute and compare: (a) a customer statement's status equals the anchored meaning of its producing action's code (or, once CDC content is undeferred, the row's own status code); (b) a customer statement's tier equals the producing row's tier where the action carries it, otherwise the previous statement's tier (per L1.omitted-facts-stand); (c) an account statement's status equals the anchored meaning of its producing action's code, or the constructed row's own status code where it is the source; (d) an account statement's tax treatment equals the producing row's value where the action carries it, otherwise the previous statement's; (e) an account statement's owner equals the producing action's customer, otherwise the previous statement's. Cite the same clauses the handoffs cite. Add them to the groups whose members they check, list them in the checked attributes' feedback, and update coverage claims minimally. Nothing else changes.
- **Current rules that must be preserved:** all clauses; cycles 2 to 9 stand.
- **Explicit holes that must remain open:** unchanged.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** unchanged.
