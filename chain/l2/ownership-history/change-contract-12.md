# Change contract, cycle 12: L1 -> L2, job ownership-history (review 11)

- **Authorized corrections:** remove from `inv.unknown_codes_held` every clause about `ce.account_changes.status_id` (statement, parallel_assumption, review_trigger) and the appended sentence and feedback entry on the `ce.account_changes.status_id -> logical.account.status` handoff; `sg.unknown-codes.coverage_claim` drops the "no anchored vocabulary at all" clause; add one `questions_for_authority` entry: whether `ce.account_changes.status_id` is constrained to the anchored status_codes (then the invariant's constructed arm becomes a vocabulary-checked pass-through) or a free pass-through (then it is not a coded field), citing `chain/ce/proposed/ce.k.constructed-status-vocabulary.md`; one sentence in the group justifying its presence in `hole.change_effective_time.blocks` despite having no deferred member. Serialize with ensure_ascii false and a trailing newline; touch nothing else.
- **Acceptance:** gate ok or question with zero problems; diff confined.
