# Change contract, cycle 11: L1 -> L2, job trade-lifecycle (market orders are not first seen late)

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md` as of cycle 10; `selected-model.json` at cycle 10.
- **Exact active change authority:** `L1.placement-moment`, amended 2026-09-17 by the business accepting `chain/ce/accepted/ce.l1.first-seen-late-market-orders.md`: "A trade whose earliest held report is not the trade's first lifecycle event is treated as placed at that report and marked as first seen late ... An order sent straight to market has no pending stage and is not first seen late when its earliest report is submitted." The anchors gained `trade_code_meanings.trade_type_codes` (TMB, TMS market; TLB, TLS limit; field t_tt_id) so the rule can be stated over an anchored code. Cache plan: `sg.placement-moment` stale by adjudication (Jev 0.68, medium, routed to review; reviewed as invalidating).
- **Authorized changes (confined to sg.placement-moment):**
  1. `logical.trade.first_seen_late.derivation.rule`: true when the first-encountered report's status is later than the trade's first lifecycle event; for a limit order (TLB, TLS) that event is PNDG, as before; for a market order (TMB, TMS) the first lifecycle event is SBMT, so a market order whose earliest report is SBMT is not first seen late, and one whose earliest report is CMPT or CNCL is. Cite L1.placement-moment (amended) and the anchored trade_type_codes. Update necessity, parallel_assumption (credible while t_tt_id on the first-encountered report is the order's type and the anchored codes are exhaustive) and review_trigger (an unanchored t_tt_id value; a market order reported PNDG).
  2. A handoff from `raw.trade_cdc.t_tt_id` into whatever the derivation needs (selector first_encounter_only), typed with a new semantic type for order type (mutation_role frozen_from_first_encounter), or state the trade type as a first-encounter input to the derivation without persisting it if the format allows; if you persist it, add attribute `order_type` to logical.trade with nullable false. Prefer the persisted attribute so the L3 audit can recompute without re-reading the raw feed for the type.
  3. `inv.trade_first_seen_late_matches_status_order`: restate over the amended rule (the expected flag recomputed from the first-encountered report's status and the order type's first lifecycle event). Keep it deterministic.
  4. `sg.placement-moment.coverage_claim`: one sentence on the market-order case and the anchored codes.
- **Current rules that must be preserved:** every other element; no other group's members or text; no hole resolved (L1.hole.trade-timestamps stays open: this says nothing about completion timing).
- **Conflict protocol reminder:** one question, omit the element, return, if the Sketch or anchors do not carry what an item needs.
- **Acceptance:** `.venv/bin/python scripts/chain_l2.py check trade-lifecycle` ok with zero problems; diff against `selected-model.json` confined to the elements enumerated here.
