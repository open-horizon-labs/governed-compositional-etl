# Review 13 (sketch reviewer, scoped): job trade-lifecycle (verdict: revise and return)

Right at the core: the two cases cycle 12 left undefined are now held, null and reported. Rejected: `sg.unknown-codes` (coverage claim enumerates readers but omits the three raw.trade_cdc selection handoffs that read cdc_flag; gap records a non-gap), `hole.market_order_seen_pending.blocks` over-claims sg.unknown-codes, `first_seen_late.review_trigger` fires on a correct projection (unknown code on a later report), `inv.trade_first_seen_late_matches_status_order` widens "not held" beyond the first-encountered report, the amended sentences on the t_st_id and t_qty handoffs contradict nullable false, and the three selection handoffs carry neither sentence nor feedback. Two questions owed: a held later report against a non-nullable outcome; a trade whose only report carries an unknown change flag. Citation strain: `inv.trade_first_seen_late_defined_or_held` argues from L1.unknown-codes, not among its group's parents. The runnable CE's assertion said one audit; the model rightly splits it into two.

Coordinator disposition: the two owed questions are answered in the Sketch, not the L2: `L1.unknown-codes` gained the clarification that a held report changes nothing and creates nothing (facts stand as last stood; a thing known only through held reports is not yet known; the hold is reported), assumed with business confirmation as the trigger; the CE document now carries those cases. Cycle 14 applies the L2 corrections and adds `L1.unknown-codes` to `sg.placement-moment`'s parents so the citation is honest.
