# Change contract, cycle 5: L1 -> L2, job trade-lifecycle

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:trade-lifecycle`. Unchanged.
- **Exact active change authority:** anchor amendment under the data-architect hat (`chain/ce/accepted/ce.k.trade-history-not-anchored.md`): `chain/anchors/sources-v1.json` now anchors `raw.trade_history` (th_t_id, th_dts, th_st_id, batch_date), the brokerage's held per-status reports for historical-load trades, and `report_order` states that such a trade's earliest held report is its earliest TradeHistory row. No L1 change.
- **Authorized corrections:** `placed_at` takes the earliest held report's own time across both anchored report sources (raw.trade_history rows and raw.trade_cdc rows), selector first_encounter_only; add the handoff `raw.trade_history.th_dts -> logical.trade.placed_at` beside the existing one, citing L1.placement-moment; `first_seen_late` considers the earliest report across both sources (its status from th_st_id or t_st_id); `owning_account_number` still comes from the Trade record's t_ca_id (TradeHistory carries no account), state that in its necessity; the ownership pin follows the corrected placed_at. Also apply the two deferred notes from review-4: add L1.hole.deletions to sg.trade-shape's gap and hole.deletions' blocks; align type.trade_first_seen_late's review trigger with the attribute's.
- **Current rules that must be preserved:** all clauses; cycle-4 elements stand unless corrected here.
- **Explicit holes that must remain open:** unchanged; completion timing stays open even though TradeHistory carries CMPT times (no clause says what completion means for reporting yet).
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** unchanged.
