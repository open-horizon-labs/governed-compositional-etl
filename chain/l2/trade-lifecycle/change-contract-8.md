# Change contract, cycle 8: L1 -> L2, job trade-lifecycle (a trade whose ownership pin does not resolve)

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:trade-lifecycle`. Unchanged. The cycle-7 model as selected (`selected-model.json`) stands.
- **Exact active change authority:** `chain/l3/duckdb-sqlmesh/positions/review-1.md`, section "Cross-engine finding". The two engine projections of your model disagree about a trade whose account or customer pin does not resolve (no statement of the owning account at or before placed_at): one keeps the trade with null values in four attributes you declare `nullable: false`; the other drops the trade entirely, outcome and all. Your model names the case only as a review trigger, so neither projection breaks a rule an audit can check, and the cross-engine compare cannot see it because the fixture has no such trade.
- **Authorized additions (deterministic invariants, disposition candidate, derived_from a subset of the group's parent clauses, with necessity, parallel_assumption, review_trigger, owner_hat; add each to the `feedback` of the attributes and handoffs it checks):**
  1. `inv.trade_ownership_pin_present` (entity logical.trade): every persisted trade carries owning_account_number, owning_account_effective_from, owning_customer_number and owning_customer_effective_from non-null. Restates the four attributes' `nullable: false` and the as_of_event_time handoffs' parallel assumptions as a checkable fact. Group sg.placement-moment.
  2. `inv.every_received_trade_persisted` (logical.trade): every trade_number reported by an anchored received source (raw.trade_cdc, raw.trade_history) appears as exactly one logical.trade row. A trade the job cannot pin is therefore a violation rather than a silent omission, and together with item 1 the unresolved-pin case fires on every engine instead of being decided differently by each. Group sg.trade-identity. Note under parallel_assumption that L1.hole.deletions bounds what a cdc_flag D report means for this count; state the invariant over reports whose cdc_flag is not D, as `inv.trade_outcome_updates_in_place` already does for quantities.
- **Authorized text corrections:** `hole.change_effective_time` (or the hole that bounds an unresolvable pin) names that a trade whose pin does not resolve is not dropped and not persisted with gaps by this job; it is surfaced through items 1 and 2 for the business to decide. The four pin attributes' review_trigger text may cite the new invariants.
- **Current rules that must be preserved:** all clauses; every cycle-7 element stands unchanged except the `feedback` lists and the hole and review_trigger texts named above. No type, entity, attribute derivation, handoff, selector, or disposition may change. Do not decide what the business would do with an unpinnable trade.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** unchanged (see `chain/anchors/DEVELOPER-CONTRACT-L1-L2.md`).
- **Acceptance:** `.venv/bin/python scripts/chain_l2.py check trade-lifecycle` status ok with zero problems; diff against `selected-model.json` confined to the elements enumerated here.
