# Deferred notes for the next authorized trade-lifecycle change (not a cycle yet)

From review-4, not defects: add L1.hole.deletions to sg.trade-shape's gap and list sg.trade-shape in hole.deletions' blocks (owning_account_number is selected by first_encounter_only from a report whose D-flagged case is open); align type.trade_first_seen_late's review trigger with the attribute's widened trigger. Both are text changes that would not re-project (group fingerprints hash parents and members).
