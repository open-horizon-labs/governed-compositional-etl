# Change contract, cycle 1: L1 -> L2, job trade-lifecycle

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:trade-lifecycle` (L1.identity, L1.as-of, L1.attribution-at-placement, L1.lifecycle-mutates-outcome, L1.constructed-scenarios) and its feedback line.
- **Exact active change authority:** none. Initial compilation. No counterexample is active for this job.
- **Upstream job you may consume:** `ownership-history`, through its selected entities in `chain/l2/ownership-history/semantic-model.json` (read `chain/l2/ownership-history/review.json` for the selected element ids; consume only selected ones). Its statements of an account are how "the account as it stood at the moment the trade was placed" is answered: an account statement is identified by (account_number, effective_from). You may not read anything else about the account.
- **Approved outputs covered by that authority:** none yet.
- **Current rules that must be preserved:** every clause the job lists, exactly as written. In particular: a trade's ownership is the account statement (and through it the customer statement) as of the moment of placement, recorded once when the trade is first seen and never changed by later reports; later reports change outcome only (status, executed price, fees, commission, tax, quantity).
- **Explicit holes that must remain open:** L1.hole.trade-timestamps (which moment is "placed" and "completed" when a later report arrives without full history), L1.hole.closed-account-activity, L1.hole.change-effective-time, L1.hole.deletions, L1.hole.securities-and-brokers, L1.hole.batch-identity where they touch this job. Do not decide that the first received row's time is the placement moment if the Sketch does not say so; if you cannot derive ownership without that decision, file the question.
- **Retained behavior that must not regress:** none yet.
- **Stable projection contracts:** `chain/anchors/semantic-model-v2.schema.json`, `chain/anchors/L2-FORMAT.md`, `chain/anchors/sources-v1.json`. Output `chain/l2/trade-lifecycle/semantic-model.json` only. `upstream_jobs: ["ownership-history"]`.
- **Forbidden shortcuts:** as in `chain/anchors/DEVELOPER-CONTRACT-L1-L2.md`; additionally, do not re-resolve ownership from the account's current statement; do not invent a surrogate key concept the Sketch does not name (a reference to an account statement is its identity pair); do not read anything under `oracle/`, `counterexamples/`, `.oh/`, `contracts/`, `docs/`, or other sketches.
- **Conflict protocol:** one precise question in `questions_for_authority`, omit the affected elements, return. Filing a question does not block the gate.
