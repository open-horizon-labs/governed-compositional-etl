# Change contract, cycle 4: L1 -> L2, job trade-lifecycle

- **Prior policy authority:** `sketches/l1-brokerage-intent-v1.md`, the clauses listed for `job:trade-lifecycle`.
- **Exact active change authority:** none new. Projection-defect corrections authorized by `review-3.md`. The gate now checks that holes' `blocks` and groups' `gap` agree in both directions.
- **Authorized corrections:** `sg.trade-shape.gap` names L1.hole.batch-identity (owning_account_number comes from the first-encountered report selected by anchored report_order, not from a report carrying which file delivered it); `sg.outcome.gap` adds L1.hole.batch-identity; `sg.placement-moment`: strike "though it does not currently block any member here" and add L1.hole.owner-change-reversions-account to the gap; `hole.closed_account_activity.blocks` lists sg.placement-moment; `logical.trade.first_seen_late`: state the premise in parallel_assumption (credible while every trade's lifecycle begins at PNDG, the first status in the anchored status_order), widen review_trigger (the business states a trade legitimately first reported at SBMT), soften necessity to "under that premise"; keep the rule, invariant, and candidate disposition. Also: `type.trade_owning_account_reference.derived_from` adds L1.lifecycle-mutates-outcome; `type.trade_first_seen_late.history_role` none; move the meta-claim out of `inv.trade_ownership_provenance_reachable.statement` into its necessity; `sg.constructed-scenarios.gap` names L1.hole.change-effective-time or the claim scopes itself to trades whose pin resolves; `hole.deletions.question` labels the current D handling (last I/U outcome stands) as provisional.
- **Current rules that must be preserved:** all clauses; cycle-3 elements stand unless corrected here.
- **Explicit holes that must remain open:** unchanged. A proposed L1 clause about market orders is filed for the business; do not act on it.
- **Stable projection contracts / forbidden shortcuts / conflict protocol:** unchanged.
