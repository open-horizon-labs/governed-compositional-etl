# Sketch review 4 (scoped): job trade-lifecycle (verdict: pass, no exclusions)

Reviewer: Opus judge. Field-by-field diff from the cycle-3 reviewed state: 17 field changes, all inside the ten authorized elements plus two coverage-claim tails that state the bound their new gap asserts. Hole/gap agreement re-derived by hand in both directions. Gate ok.

All review-3 findings addressed; the constructed-scenarios scoping was the better of the two offered options. first_seen_late selected as a review flag with its premise and mis-marking direction stated in the model; excluding it would leave half of L1.placement-moment unmodeled.

Notes for a future authorized change, not defects: sg.trade-shape's gap and hole.deletions' blocks should also record that owning_account_number is taken by first_encounter_only from the same first report whose D-flagged case is open; type.trade_first_seen_late's review trigger should match the attribute's widened trigger.
