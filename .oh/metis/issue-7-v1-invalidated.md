---
id: issue-7-v1-invalidated
status: accepted-learning
owner_hat: research-sponsor
---

# V1 experiment invalidated

The research-sponsor hat invalidates the complete v1 run, including its `revise` decision. The harness compared synthetic evidence-selection behavior rather than actual injected, repaired, and re-executed ETL failures. The runner could read committed held-out answer submissions, and its deterministic work count was mislabeled as milliseconds.

V1 remains in history only as failed-harness evidence. It contributes no numerator, denominator, threshold result, held-out result, or adoption signal to v2.
